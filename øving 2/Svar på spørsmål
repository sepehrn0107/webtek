Spørsmål:
1. Why does inline style CSS override rules defined in style elements and external
stylesheets?
      Det finnes hierarki i CSS h html som gjør at kommandoene som er viktigere
      eller høyere prioritert blir lest mens andre blir overkjørt. Hierarkiet
      består av: inline style, embedded style, external stylesheet, brukerdefinert
      tyle og til slutt browser default. Hierarkiet startermed inline style som øverst
      og derfor overkjører inline style css andre definerte elementer.

  2. Give a brief example of when to use ID ( #id ) and when to use classes ( .class ) in CSS
      Ved å gi hvert element en ID, kan man referere til det elementet med id i senere tidspunker,
      som for eksempel i CSS. Ved å dele inn seksjoner og gi hvert av dem en ID, kan man
      for eksempel endre font, størrelse på skrift eller bilde, uten at andre elementer blir påvirket.
      Ved å bruke .class, kan man referere til flere elementer enn det man kan ved å bruke ID
    3. What does RGBA mean and what colors can you express with it?
      RGBA står for "red, green, blua alpha" og man kan definere hvilken som helst farge ved å bruke RGBA.
    4. Why do we include CSS files inside the head element and not inside the body element?
      ved å inkludere CSS-fil i Head så definerer man at den filen skal brukes gjennom hele koden, men selve
      koden som ligger i Head vil ikke vises. man refererer til en CSS fil, akuratt som man
      også definerer hvordan nettleseren skal lese språket i html (<meta charset="utf-8" />)
      5. What CSS selector matches all the p elements inside the article element in the
  following HTML?
      Child-selector
      article > p {
      
      }
